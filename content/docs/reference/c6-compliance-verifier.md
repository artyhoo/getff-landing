---
title: compliance-verifier
description: Reviews PR description §1.7 Forward-check and Backward-check sections for substantive evidence — file:line citations, sweep completeness, exemption quality. Reports; does not fix.
---
<!-- provenance: framework @ b069c59328aa8e08671d3a19fd25efa618dfc1c5 2026-09-11, re-read 2026-09-12 · raw draft (beta-docs-encyclopedia) —
     polish pass pending; re-derive from the anchors below, do not hand-edit prose -->

# compliance-verifier

**Status:** shipped-beta · **Ships to:** npm lane — delivered into the consumer's `.claude/agents/` by the setup delivery loop (`setup.d/20-agents.sh:24`) · **Fires at:** dispatched as a Claude Code sub-agent (frontmatter `name:`; the session's agent list carries its one-line description)

## What it is

"> **Authoritative for:** `compliance-verifier` sub-agent prompt — PR description §1.7 section" substance review; reporting-only (`agents/compliance-verifier.md:9-10`). It reviews PR §1.7 Forward-check/Backward-check evidence: file:line citations, sweep completeness, exemption quality (frontmatter description, line 3). Division of labour with the deterministic layer: "The point of this role: Wave 8.1's deterministic regex (`.github/workflows/discipline-self-check.yml`)" verifies sections exist; this agent verifies citations are real (`agents/compliance-verifier.md:23-27`). A plugin twin exists at `plugin/agents/compliance-verifier.md` — a generated byte-identical copy for the plugin marketplace channel (verified line-for-line against the agent file at authoring time).

## How it works

- "## What you check" (line 39) — five YES/NO items over the PR body ("Work through the PR description in the open tab or from `gh pr view <number> --json body`. For", line 41).
- Forward-check per-layer coverage (line 44 heading): "For each applicable layer (R1-R20 code-level rules · Principles 01-N · Capability-commit gate ·" (line 47); theatre tell at line 54: "**Theatre tell:** «R1-R20 checked — all compliant» with no diff/file ref."
- Citation integrity: "Pick 2-3 of the `file.ext:line` citations; `Read` the cited locations. Does the cited line" (line 57) — REVISE if off by >±3 lines or misrepresenting.
- Backward-check sweep completeness: "The Backward-check must enumerate the **complete set** of existing artefacts under the new" (line 65); "**Real sweep:** concrete command + output count + line numbers (e.g." (line 68).
- Exemption mechanism needs a paired negative test (line 82: "without a paired negative test in the same diff."); trailer checks substance parity against the pre-push hook's presence check (lines 86-89).
- Anti-patterns (line 91 heading), including "- **`#discipline-theatre`** — §1.7 section contains ≥40 chars of fluent prose asserting" (line 93). "## Final verdict" (line 116); STOP is not used — "a present-but-hollow §1.7 is REVISE." (line 134, with line 133: "STOP-level (K1/K2/K5-class) is not used by this agent — a missing §1.7 section entirely is the").

## Satellites & companions

<!-- what upstream/peer capability it USES, what it ADAPTS, what it ADDS over them -->

- **USES:** reviewer-discipline clauses (line 33 heading) and the §1.7 discipline owned by `.claude/rules/phase-research-coverage.md` (lines 44, 91).
- **ADAPTS:** the dispatch-input-checker verdict vocabulary — line 16: "> Reach + restoration trigger in map §4.2. Verdict vocab GO/REVISE/STOP per".
- **ADDS:** the substance layer over the regex gate — "This is the **two-AI review pattern**: the implementing session wrote the §1.7 sections in the" same head that wrote the diff (line 29). Explicit non-claim: "- You do **not** make a judgment on the diff's _correctness_ (that is `review-sidecar`'s role)." (line 139).
- Census family satellites: **C10** (review-sidecar — the diff-correctness peer this agent defers to); the plugin twin carries the same prompt on the marketplace channel.

## Anchors

<!-- the file:line list, each anchor with its line's content quoted at authoring time -->

- `agents/compliance-verifier.md:2` — `name: compliance-verifier`
- `agents/compliance-verifier.md:3` — `description: Reviews PR description §1.7 Forward-check and Backward-check sections for substantive evidence — file:line citations, sweep completeness, exemption quality. Reports; does not fix.`
- `agents/compliance-verifier.md:9` — `> **Authoritative for:** `compliance-verifier` sub-agent prompt — PR description §1.7 section`
- `agents/compliance-verifier.md:39` — `## What you check`
- `agents/compliance-verifier.md:56` — `### 2. Forward-check: citation integrity (spot-check 2-3)`
- `agents/compliance-verifier.md:65` — `The Backward-check must enumerate the **complete set** of existing artefacts under the new`
- `agents/compliance-verifier.md:102` — `## How to report (one block per issue, do not bundle)`
- `agents/compliance-verifier.md:116` — `## Final verdict`
- `agents/compliance-verifier.md:139` — `- You do **not** make a judgment on the diff's _correctness_ (that is `review-sidecar`'s role).`
- `plugin/agents/compliance-verifier.md:2` — `name: compliance-verifier`
- `setup.d/20-agents.sh:24` — `for f in "$PKG_ROOT"/agents/*.md; do`

All anchors at framework pin `b069c59328aa8e08671d3a19fd25efa618dfc1c5` (fetched 2026-09-11 from `artyhoo/getff` staging; ancestry of census pin `a1337cb301` verified).
