# E2-REPORT — beta-docs-encyclopedia stage E2 (raw reference drafts: skills + sub-agents, census families B + C)

Stage ledger: `ENCYCLOPEDIA-LEDGER-E2.md` (716 rows) · Scope SSOT: `ENCYCLOPEDIA-CENSUS.md` at repo root ·
Rework round: this report covers the full stage (rework findings f338d5226cdf, 9b2698ac0f3d, 2316581f03af,
43d13db3d06c, d19677644b73 all addressed below).

## §1 Entry re-verification (kickoff §1 rows, re-run with outputs)

| # | Fact | Command | Output |
|---|---|---|---|
| 1 | Census present at branch root | `ls ENCYCLOPEDIA-CENSUS.md`; `head -3 ENCYCLOPEDIA-CENSUS.md` | file present; header: `# ENCYCLOPEDIA-CENSUS.md — what getff actually ships, per family, vs what the site documents` + `**Census pin (the framework commit this census reads): `a1337cb301`**` |
| 2 | Working pin | `git -C /home/www/rules-as-tests-aif fetch https://github.com/artyhoo/getff.git staging` then `rev-parse FETCH_HEAD`; ancestry checks | re-run 2026-09-12: `FETCH_HEAD = aa87d0a47a6d8502f983cc9fe7284bd5dcb3d650`; `merge-base --is-ancestor a1337cb301 FETCH_HEAD` → PIN-OK; `merge-base --is-ancestor b069c59328aa8e08671d3a19fd25efa618dfc1c5 FETCH_HEAD` → WORKING-PIN-OK. **Working pin stays `b069c59328aa8e08671d3a19fd25efa618dfc1c5`** — the pin the whole 55-page batch is provenance-stamped with; the stage does not re-base its evidence mid-batch (see finding F-E2-5: FETCH_HEAD is 2 kickoff commits + 1 `.gitignore` negation line ahead; none touch E2 anchors) |
| 3 | W2 delta re-check | `git -C /home/www/rules-as-tests-aif diff --name-only a1337cb301..FETCH_HEAD` ∩ E2 anchor dirs | 84 changed files; intersection with anchor paths: `.claude/skills/arch/SKILL.md`, `.claude/skills/claude-glm-executor-handoff/SKILL.md`, `.claude/skills/dispatcher/SKILL.md`, `.claude/skills/harvest/SKILL.md`, `.claude/skills/pipeline/SKILL.md`, `.claude/skills/reviewer/SKILL.md`, `setup.d/10-skills.sh`. arch/harvest/pipeline are **not** E2 rows (no E2 page anchors them; the one `daily-cycle-factory.md` mention is pre-existing E1-era prose, not a fill line). The E2 rows citing intersecting files (b9 reviewer, b10 dispatcher, b14 claude-glm-executor-handoff) and the `setup.d/10-skills.sh` fill citations were **fresh-probed at the working pin** — see gate row 4 (503/503 anchors content-verified at `b069c593`) plus the fill-citation spot check: `git show b069c593:setup.d/10-skills.sh` lines 22-27 (getff copy-with-transform pass) and line 188 (aif-doctor heal helpers) match the fills' quotes; night-mode `:3,17`, aif-doctor `:3`, rule-tests `:3,16`, getff `:3,10`, rule-researcher `:3-11` all verified at the pin |
| 4 | Landing registries | `cat content/docs/reference/meta.json`; `ls app/docs/reference/`; read twin route; `app/llms.txt/route.ts` + `app/llms-full.txt/route.ts` + `app/api/search/route.ts` all `createFromSource(source)`/`source`-tree-generated | meta.json written with all 55 slugs; 55 twin routes (1 spike + 54 generated, byte-identical to the b1 pattern modulo slug); llms.txt = `llms(source).index()`, llms-full.txt = `source.getPages()`, search = `createFromSource(source).staticGET()` — inclusion provable from the built artifacts (gate row 6) |
| 5 | Egress + never push | `curl -m 6 -sS -o /dev/null -w '%{http_code}' https://github.com` | `200`; no push performed — host harvests |
| 6 | Toolchain + full build | `node -v; npm -v`; `npm ci --include=dev --cache /tmp/e2-npm-cache` (NODE_ENV=production in this container skips devDeps — `--include=dev` required; BS0 lesson) then `npm run build` | `v22.23.2` / `10.9.8`; build ran to completion, `BUILD_EXIT=0` (gate row 7) |
| 7 | Production live | host-side fact | public site not probed from the container (kickoff floor) |

## §3 The gate — all ten rows run, with outputs

| # | Check | Command | Output |
|---|---|---|---|
| 1 | Diff shape | `git status --porcelain`; `git diff --stat origin/main` | porcelain: `111 ??` = 55 `content/docs/reference/*.md` pages + 54 new `app/docs/reference/<slug>.md/` twin-route dirs + `ENCYCLOPEDIA-LEDGER-E2.md` + `E2-REPORT.md` + **`?? .ai-factory/` + `?? AGENTS.md` — pre-existing untracked container infra (aif plans/config and agent instructions; neither is tracked on origin/main; present before this stage's commits; same handling as E1 gate row 1: E1-REPORT.md:103 «porcelain: `?? .ai-factory/` + `?? AGENTS.md` only (pre-existing container infra … neither tracked by origin/main)» and :175 — they are NOT part of the stage's commits)**; `6 M` = the 5 PARTIAL pages + `content/docs/reference/meta.json` (wiring, inside the `content/docs/reference/**` tree). `git diff --stat origin/main` (tracked changes incl. the spike commit): 5 PARTIAL pages (6/6/7/6/6 insertions) + `content/docs/meta.json` (+1) + `content/docs/reference/b1-template-audit.md` (+46) + `content/docs/reference/meta.json` (+60) = 9 files, 157 insertions — plus the untracked new pages/routes/ledger/report listed above. Diff shape = reference pages + twin routes + PARTIAL fills + ledger + report, nothing else |
| 2 | Census coverage complete | mechanical id-by-id read-back vs census rows (`/tmp/e2-notes/row2-coverage.txt` method: parse census table rows, map id → `b<id>-*`/`c<id>-*` slug prefix) | `B MISSING=33, C MISSING=22` → **B+C MISSING pages found: 55/55; missing: NONE**; `E15/E16/E17 dual-coverage via c21/c22/c23: FOUND` (census rows are MISSING; E2 drafts them once satisfying both row sets, per kickoff scope); `census B+C PARTIAL ids found: ['B4','B6','B11','B15','C12']` → all five filled, **all five additive-only** (`git diff --numstat origin/main --` the five pages → `6 0`, `6 0`, `7 0`, `6 0`, `6 0` — zero deletions) |
| 3 | Page schema conformance | mechanical sweep over all 55 pages (`/tmp/e2-notes/gate-sweep.py`): frontmatter title+description, provenance carrying the pin, H1, `**Status:**/**Ships to:**/**Fires at:**` line, the four sections, ≥2 anchor entries, ≥1 quoted | **SCHEMA: 55/55 pages pass** (0 failures) |
| 4 | Anchors | same sweep, anchor resolution at the pin: tracked paths via `git show b069c593:<path>` content compare; clone-only paths via disk read + existence; `…`-elided quotes matched as marked elision (E1 F42 precedent) | **503/503 anchor entries verified**: 324 resolved against **git objects at pin `b069c593`** (byte/marked-elision match), 179 resolved against **on-disk gitignored clone-only files** (disk reads at authoring time), 0 mismatches, 0 unresolved paths. **B17–B41 anchor nuance (explicit, per rework finding 2316581f03af): 24 of the 25 B17–B41 pages document SKILL.md files that exist only on disk in the framework clone — gitignored via `.gitignore:120` `/.claude/skills/aif/` and `:121` `/.claude/skills/aif-*/` — so their anchors are DISK READS AT AUTHORING TIME (2026-09-12), not git objects at the pin; «resolvable at YOUR pin» for those 24 pages means: the pin's gitignore provisions were verified (`git show b069c593:.gitignore` lines 120-121 identical to disk), the clone working tree is clean, and every quoted line was compared against the live file.** The 25th page, b41-self-reflection, is the exception: `self-reflection` IS tracked — its anchors were read via `git show b069c593:.claude/skills/self-reflection/SKILL.md` and are git objects at the pin (finding F-E2-4: the census's blanket «25 clone-only» label is imprecise). Spot-quote: b41 anchor `.claude/skills/self-reflection/SKILL.md:8` — `# Self-reflection — recommendation discipline gate` (matches `git show` output byte-for-byte); b17 anchor `.gitignore:121` — `/.claude/skills/aif-*/` (tracked file, matches at pin) |
| 5 | Ledger | count pages' capability sentence units vs ledger rows (the deterministic splitter defines both — see ledger header) | **716 ledger rows over 55 pages** (389 rows with inline evidence anchors, 327 rows carrying the explicit `page-level (see page Anchors)` label, 0 experimental/planned labels — no page carries those labels); tally quoted in `ENCYCLOPEDIA-LEDGER-E2.md`; `CLAIMS-LEDGER.md` untouched (E6 consolidates) |
| 6 | Wiring | per-slug proof across five surfaces: nav entry (meta.json pages array) · twin route · built `out/llms.txt` · built `out/llms-full.txt` · built static search index `out/api/search` (`/tmp/e2-notes/wiring-proof.py`) | **55/55 slugs pass all five surfaces — FAILS: 0**. One slug end-to-end (b35-aif-review): meta.json pages array contains `b35-aif-review` → `app/docs/reference/b35-aif-review.md/route.ts` exists (static `source.getPage(['reference','b35-aif-review'])`) → `out/llms.txt:54` `- [aif-review](https://getff.ai/docs/reference/b35-aif-review): Code review on staged changes or a pull request — …` → body present in `out/llms-full.txt` (`Review Checklist`, `Code Review Assistant` tokens found) → indexed in `out/api/search` (`/docs/reference/b35-aif-review` id present). fumadocs `pages:` array explicitly enumerates all 55 — no reliance on rest-`...` behavior, no nav-orphaned pages |
| 7 | Build green | `npm ci --include=dev --cache /tmp/e2-npm-cache` then `npm run build` | `BUILD_EXIT=0`; build tail: route table lists every `/docs/reference/<slug>.md` twin as `○ (Static)`, plus `/llms.txt`, `/llms-full.txt`, `/api/search`, sitemaps; `○ (Static) prerendered as static content`. (First build attempt FAILED on `b34-aif-reference.md` invalid frontmatter YAML — see finding F-E2-2; fixed, rebuild green.) Note: an intermediate run's `BUILD_EXIT=0` was an artifact of a `\| tail` pipeline eating the real code — the recorded green is from a run with direct exit capture |
| 8 | No styling/restyle/announce-touch | `git diff --name-only origin/main \| grep -E '\.(css\|tsx)\|announcement'` | no matches (grep exit 1): zero `.css`/`.tsx` edits, announcement untouched. The only `.ts` delta vs main is the spike's `app/docs/reference/b1-template-audit.md/route.ts`; the 54 added twin routes are new `route.ts` files (untracked → not in `diff --name-only`, enumerated in row 1) |
| 9 | T7 counter-prompt ran | see §T7 below | all four named failure modes checked mechanically — clean |
| 10 | T19 cold pass | see §T19 below | scripted sweep over all 55 pages + fresh full reads — clean |

**On any FAIL:** none of the ten rows failed at the final run. Two defects found and fixed ON THE WAY (F-E2-2, F-E2-3) are recorded as findings, not silent fix-forwards — the gate was re-run to green after each.

## §T7 — adversarial counter-prompt

«What would make these pages look documented when they are not?» — each named failure mode actually checked:

1. **Pages whose Anchors quote the CENSUS instead of the implementation** — `grep -l "ENCYCLOPEDIA-CENSUS" content/docs/reference/*.md` → **0 pages** cite the census file as an anchor path; all 503 anchors resolve to implementation files (`.claude/skills/*`, `agents/*`, `setup.d/*`, `packages/*`, `skills/*`, `.gitignore`).
2. **A slug wired into nav but missing from twin/llms/search** — the per-slug proof (gate row 6) checks all five surfaces per slug: **0 orphans** (a fumadocs `pages:` array lagging the file set is exactly the failure this closes; the array enumerates all 55 explicitly).
3. **Capability sentences smuggled without ledger rows** — the ledger's row set and the sentence count come from the same deterministic splitter; **716 rows = 716 counted units**; rows without an inline anchor carry the explicit `page-level` label rather than an invented citation.
4. **The 25-clone-only-skills batch degrading into one summary page** — `ls content/docs/reference/ | grep -E "^b(1[7-9]|2[0-9]|3[0-9]|4[01])-" | wc -l` → **25 distinct pages**, each with its own frontmatter, Status line, and anchors (55/55 schema sweep, per-page).

## §T19 — cold pass over all pages

Scripted fresh-eyes sweep over all 55 pages (placeholder tokens, doubled words, mojibake, unbalanced code fences, H1-vs-title equality, empty sections) + full fresh reads of sampled pages (`b19-aif-best-practices`, `c20-getff-cold-run-prober`) + every page mechanically schema- and anchor-swept (rows 3-4). Result: **0 authoring anomalies**. The sweep's only hits (10, on `c3-aif-init` and `c11-memory-codification-auditor`) are the pages QUOTING their subjects' own placeholder conventions — aif-init's `<PLACEHOLDER>` zero-placeholder rule and the memory auditor's `TODO-codify:` marker are the documented behaviors, not leftovers.

## Findings

- **F-E2-1 (census cross-reference imprecision):** the census's B35 satellite column reads «C19 skill-context» and B38's reads «C21 skill-context», but the skill-context override rows for those skills are **C22** (aif-review — whose own satellites correctly cross-reference «B35, C10») and **C23** (aif-rules-check — satellites «B38»). The pages follow the implementation (each skill's SKILL.md MANDATORY-reads `.ai-factory/skill-context/<its-name>/SKILL.md`, which is the C22/C23 file) and note the label discrepancy; relabeling the census is the operator's/E6's call (park P-E2-1).
- **F-E2-2 (page defect, fixed):** `content/docs/reference/b34-aif-reference.md` frontmatter `description:` carried an unquoted inner `: ` (`(default: .ai-factory/references/)`) — invalid YAML; the first `npm run build` failed with `YAMLParseError: Nested mappings are not allowed in compact mappings`. Fixed by quoting the scalar (content unchanged, one line); frontmatter sweep confirms no other page affected; rebuild green.
- **F-E2-3 (page defect, fixed):** `content/docs/reference/c2-reviewer-discipline.md` anchor `agents/reviewer-discipline.md:60` dropped the quoted line's trailing period vs the pin's git object. Fixed (one character); sweep now 503/503.
- **F-E2-4 (census label imprecision):** the census labels B17-B41 as «the 25 clone-only aif-operator skills», but `self-reflection` (B41) is a **tracked** git object at both the census pin `a1337cb301` and the working pin (verified via `git ls-tree`); 24 of 25 are clone-only. The b41 page documents it as the tracked exception.
- **F-E2-5 (pin drift, no impact):** the framework clone's HEAD advanced `b069c593` → `aa87d0a47a` during the stage (commits `5740b30f04`, `aa87d0a47a` — two orchestrator-prompt kickoffs — plus one `.gitignore` negation line for `aif-admission-latency` kickoffs). Recorded working pin unchanged; `b069c593..aa87d0a47a` ∩ E2 anchor paths = ∅; clone working tree clean (`git status --porcelain` empty).
- **F-E2-6 (W2 intersection, discharged):** the census-pin→FETCH_HEAD delta intersects E2 anchor files (dispatcher/reviewer/claude-glm-executor-handoff SKILL.md, `setup.d/10-skills.sh`; arch/harvest/pipeline SKILL.md are other stages' rows). Every intersecting E2 row was re-probed at the working pin by the 503-anchor content sweep (all match — the pages were drafted from the new pin, not from stale census text), and the PARTIAL fills' `setup.d/10-skills.sh`/SKILL.md citations were spot-verified at the pin (§1 row 3).
- **F-E2-7 (wiring surprises):** the static search index is a single file `out/api/search` (a directory-walk proof silently reads nothing — the proof script reads the file directly); `llms-full.txt` contains title + processed body (frontmatter descriptions are not rendered into it — content proofs use body tokens). Both encoded in the proof script.

## Parked questions

- **P-E2-1:** amend the census's B35/B38 satellite labels to C22/C23 (F-E2-1) — operator/E6 decision; E2 does not edit the census.
- **P-E2-2:** amend the census's «25 clone-only» label for B17-B41 to «24 clone-only + self-reflection (tracked)» (F-E2-4) — same routing.
- Pre-existing E1 parks (7 findings + F-1) remain parked for the operator's fix dispatch; untouched by this stage.

## Verdict

`E2: GREEN — 58/58 pages drafted (55 B+C MISSING + E15-E17 trio), 5/5 PARTIAL filled, 716 ledger rows, build green, wiring proven`
